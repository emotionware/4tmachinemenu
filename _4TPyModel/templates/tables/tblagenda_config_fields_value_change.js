<script>
$(document).ready(function () { $("#idagenda").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#idagenda").val(); 
 });}); 

 $(document).ready(function () { $("#id").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#id").val(); 
 });}); 

 $(document).ready(function () { $("#status").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#status").val(); 
 });}); 

 $(document).ready(function () { $("#about").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#about").val(); 
 });}); 

 $(document).ready(function () { $("#adress").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#adress").val(); 
 });}); 

 $(document).ready(function () { $("#company").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#company").val(); 
 });}); 

 $(document).ready(function () { $("#name").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#name").val(); 
 });}); 

 $(document).ready(function () { $("#style").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#style").val(); 
 });}); 

 $(document).ready(function () { $("#calendar").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#calendar").val(); 
 });}); 

 $(document).ready(function () { $("#start").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#start").val(); 
 });}); 

 $(document).ready(function () { $("#end").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#end").val(); 
 });}); 

 $(document).ready(function () { $("#tabla").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#tabla").val(); 
 });}); 

 $(document).ready(function () { $("#idobjeto").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#idobjeto").val(); 
 });}); 

 $(document).ready(function () { $("#usuario").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#usuario").val(); 
 });}); 

 $(document).ready(function () { $("#allDay").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#allDay").val(); 
 });}); 

 $(document).ready(function () { $("#background").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#background").val(); 
 });}); 

 $(document).ready(function () { $("#borderColor").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#borderColor").val(); 
 });}); 

 $(document).ready(function () { $("#color").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#color").val(); 
 });}); 

 $(document).ready(function () { $("#draggable").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#draggable").val(); 
 });}); 

 $(document).ready(function () { $("#hidden").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#hidden").val(); 
 });}); 

 $(document).ready(function () { $("#recurrencePattern").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#recurrencePattern").val(); 
 });}); 

 $(document).ready(function () { $("#recurrenceException").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#recurrenceException").val(); 
 });}); 

 $(document).ready(function () { $("#resizable").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#resizable").val(); 
 });}); 

 $(document).ready(function () { $("#resourceId").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#resourceId").val(); 
 });}); 

 $(document).ready(function () { $("#readOnly").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#readOnly").val(); 
 });}); 

 $(document).ready(function () { $("#tooltip").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#tooltip").val(); 
 });}); 

 $(document).ready(function () { $("#timeZone").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#timeZone").val(); 
 });}); 

 $(document).ready(function () { $("#OBSERVACIONES").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#OBSERVACIONES").val(); 
 });}); 

 $(document).ready(function () { $("#IDCOTIZACION").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDCOTIZACION").val(); 
 });}); 

 $(document).ready(function () { $("#IDPROSPECTO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDPROSPECTO").val(); 
 });}); 

 $(document).ready(function () { $("#IDABONO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDABONO").val(); 
 });}); 

 $(document).ready(function () { $("#DESDE_FECHA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#DESDE_FECHA").val(); 
 });}); 

 $(document).ready(function () { $("#DESDE_HORA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#DESDE_HORA").val(); 
 });}); 

 $(document).ready(function () { $("#HASTA_FECHA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#HASTA_FECHA").val(); 
 });}); 

 $(document).ready(function () { $("#HASTA_HORA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#HASTA_HORA").val(); 
 });}); 

 $(document).ready(function () { $("#ELIMINADO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#ELIMINADO").val(); 
 });}); 

 $(document).ready(function () { $("#BLOQUEADO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#BLOQUEADO").val(); 
 });}); 

 $(document).ready(function () { $("#FECHAALTA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#FECHAALTA").val(); 
 });}); 

 </script>
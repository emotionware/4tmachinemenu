<script>
$(document).ready(function () { $("#IDPERSPECTIVAEXTERNA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDPERSPECTIVAEXTERNA").val(); 
 });}); 

 $(document).ready(function () { $("#NOMBRE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#NOMBRE").val(); 
 });}); 

 $(document).ready(function () { $("#TABLA_FUENTE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#TABLA_FUENTE").val(); 
 });}); 

 $(document).ready(function () { $("#CAMPO_ACCESO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#CAMPO_ACCESO").val(); 
 });}); 

 </script>
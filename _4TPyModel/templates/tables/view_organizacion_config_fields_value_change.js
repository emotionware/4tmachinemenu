<script>
$(document).ready(function () { $("#IDAGENTE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDAGENTE").val(); 
 });}); 

 $(document).ready(function () { $("#AGENTE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#AGENTE").val(); 
 });}); 

 $(document).ready(function () { $("#EMAIL_AGENTE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#EMAIL_AGENTE").val(); 
 });}); 

 $(document).ready(function () { $("#IDSUPERVISOR").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDSUPERVISOR").val(); 
 });}); 

 $(document).ready(function () { $("#SUPERVISOR").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#SUPERVISOR").val(); 
 });}); 

 $(document).ready(function () { $("#EMAIL_SUP").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#EMAIL_SUP").val(); 
 });}); 

 $(document).ready(function () { $("#IDGERENTE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDGERENTE").val(); 
 });}); 

 $(document).ready(function () { $("#GERENTE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#GERENTE").val(); 
 });}); 

 $(document).ready(function () { $("#EMAIL_GERENTE").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#EMAIL_GERENTE").val(); 
 });}); 

 </script>
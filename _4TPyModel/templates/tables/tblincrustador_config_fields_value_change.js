<script>
$(document).ready(function () { $("#IDINCRUSTADOR").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDINCRUSTADOR").val(); 
 });}); 

 $(document).ready(function () { $("#IDSYSCOMPONENT").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDSYSCOMPONENT").val(); 
 });}); 

 $(document).ready(function () { $("#TABLA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#TABLA").val(); 
 });}); 

 $(document).ready(function () { $("#CAMPO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#CAMPO").val(); 
 });}); 
 
  $(document).ready(function () { $("#ABAJO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   var value = $("#ABAJO").val(); 
   
    
   

 });}); 
 
   $(document).ready(function () { $("#ARRIBA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   
   var value = $("#ARRIBA").val(); 
   
    
    
 });}); 
 
   $(document).ready(function () { $("#ENMEDIO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
    var value = $("#ENMEDIO").val(); 
    
   
    
 });}); 




 </script>